<?xml version="1.0" encoding="utf-8"?>
<!--
    Only views RemoteViews supports: LinearLayout, TextView, ProgressBar, ImageView.
    Widths use weights rather than fixed dp so horizontal resizing reflows instead of
    clipping - the figures are the one thing that must never be cut off.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="14dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/widget_period"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFE9B949"
            android:textSize="13sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:text="September" />

        <TextView
            android:id="@+id/widget_amount"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="#FFFFFFFF"
            android:textSize="19sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:text="₹18,240 of ₹42,000 in" />

        <LinearLayout
            android:id="@+id/widget_bar_row"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="7dp"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ProgressBar
                android:id="@+id/widget_bar"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="0dp"
                android:layout_height="9dp"
                android:layout_weight="1"
                android:max="100"
                android:progress="43"
                android:progressDrawable="@drawable/widget_progress" />

            <TextView
                android:id="@+id/widget_percent"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="9dp"
                android:textColor="#FFFFFFFF"
                android:textSize="13sp"
                android:text="43%" />
        </LinearLayout>

        <TextView
            android:id="@+id/widget_footer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dp"
            android:textColor="#B3FFFFFF"
            android:textSize="12sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:text="₹23,760 left · 8 days" />
    </LinearLayout>

    <ImageView
        android:id="@+id/widget_add"
        android:layout_width="46dp"
        android:layout_height="46dp"
        android:layout_marginStart="12dp"
        android:background="@drawable/widget_button"
        android:padding="11dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_widget_add"
        android:contentDescription="@string/widget_add_expense" />
</LinearLayout>
